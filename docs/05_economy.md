# Economy (concept)

- Compute taxation: reserve a fixed % of cycles for public work.
- Computational currency: credits represent the ability to request resources.

Implementation is intentionally deferred; the crate provides the governance/state primitives first.
